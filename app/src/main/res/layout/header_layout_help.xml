<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.CardView
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    android:foreground="?android:attr/selectableItemBackground"
    card_view:cardCornerRadius="@dimen/cardview_default_radius"
    android:layout_marginLeft="8dp"
    android:layout_marginRight="8dp"
    android:layout_marginTop="4dp"
    android:layout_marginBottom="4dp"
    card_view:cardElevation="0dp"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <LinearLayout android:id="@+id/header_top"
        android:orientation="horizontal"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@android:color/white"
        android:paddingTop="20dp"
        android:paddingBottom="20dp"
        xmlns:android="http://schemas.android.com/apk/res/android">

        <Button
            android:id="@+id/buttonTest"
            style="@style/CustomFlatButtonStyle"
            android:layout_weight="1"
            android:drawableTop="@drawable/ic_pen_grey600_36dp"
            android:drawablePadding="5dp"
            android:visibility="gone"
            android:text="MOCK TEST" />

        <View
            android:layout_width="1dp"
            android:layout_height="fill_parent"
            android:visibility="gone"
            android:background="@android:color/white" />

        <Button
            android:id="@+id/buttonAskExpert"
            style="@style/CustomFlatButtonStyle"
            android:layout_weight="1"
            android:drawableTop="@drawable/ic_help_circle_outline_grey600_36dp"
            android:drawablePadding="5dp"
            android:text="ASK EXPERT" />

    </LinearLayout>

</android.support.v7.widget.CardView>